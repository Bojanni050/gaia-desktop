{
  "name": "gaia-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "A lifelong personal intelligence. Desktop shell — a first-class client of Gaia Cloud, never a wrapper around Gaia Web.",
  "scripts": {
    "dev:desktop": "tauri dev",
    "build:desktop": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
